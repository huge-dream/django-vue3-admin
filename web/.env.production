# 线上环境
ENV = 'production'

# 线上环境接口地址
# 说明：项目内接口多以 /api/... 作为绝对路径（如 /api/captcha/、/api/init/settings/）。
# 因此 baseURL 不应再额外带 /api，避免拼接成 /api/api/...
# VITE_API_URL = '/'
VITE_API_URL = 'http://192.168.80.90:8000'

# 是否启用按钮权限
VITE_PM_ENABLED = true
